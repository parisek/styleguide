{#
name: "Plain Doc"
#}
<div class="plain-doc">This doc is content-only markup — no component_*/page_* call, no include of a component. It exercises MUST-FIX 1: renderObserved() must not throw on a legitimate content-only doc fixture.</div>
