{#
category: "Basic"
kind: utility
#}
{# Valid YAML metadata with no `name:` — the exact shape `unindexed` exists to
	catch. Deliberately OUTSIDE `_partials/`, because partial directories are
	skipped by the walk and a fixture in one can no longer reach this rule. #}
<div class="nameless"></div>
