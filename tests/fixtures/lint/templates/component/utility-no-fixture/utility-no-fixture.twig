{#
name: "Utility No Fixture"
kind: utility
description: "A utility renders whatever it is handed, so it has no stable appearance to pin — exempt from `no-fixture` by design."
#}
<img src="{{ content.src }}" alt="">
