{# Ordinary leading code comment: this component intentionally has NO
   metadata front-comment, because its <id>.yaml supersedes it (ADR 0007).
   Parsed as YAML it is nonsense — which is the point of the test. #}
{% extends "@component/component/component.twig" %}
